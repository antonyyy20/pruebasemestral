<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Debug/local API over HTTP (emulator 10.0.2.2, adb reverse, LAN IP) -->
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
